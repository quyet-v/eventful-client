/* eslint-disable react/prop-types */
import React from 'react';

function ProfileOption({ logo }) {
  return (
    <div className="container">
      {logo}
    </div>
  );
}

export default ProfileOption;
